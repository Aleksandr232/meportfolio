import easterBreaker from "easter-break";

 const EasterBraker = () => {
    return (
        <button className="btn" onClick={easterBreaker}>Убрать</button>

    );
  };
  export default EasterBraker